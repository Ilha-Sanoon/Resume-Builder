# Folio.field — Angular Frontend
Fresh standalone Angular frontend for a professional Resume Builder.

## Run
npm install
npm start

Open the localhost address shown by Angular CLI.

## Flow
Landing → Sign Up / Sign In → Dashboard → Template Selection → CV Editor → Live Preview → Save → PDF/JPG/PNG download UI.

The frontend is intentionally independent from the existing backend so the UI can be tested first.
